"""GUI layer (optional; requires PySide6).

* :mod:`theme`           -- shared dark/amber palette + stylesheet.
* :mod:`gui`             -- MainWindow, StageIndicator, run_app.
* :mod:`settings_dialog` -- per-group config editor (opened as a separate window).
* :mod:`image_pane`      -- sample-overview image: load, rotate, calibrate,
                            click-to-move within limits.
"""
