# module.toml -- how the suite finds this module (INSTRUMENT_MODULE_GUIDE.md section 11).
# Identity only; the variables come from the running service's `describe`.

[module]
key = "ppms"
name = "PPMS DynaCool"
description = "Quantum Design DynaCool: magnetic field, temperature, chamber (MultiVu)"
category = "environment"
tags = ["field", "temperature", "cryostat", "Quantum Design"]
icon = "icon.svg"
order = 130

[ports]
cmd = 5579
pub = 5580

[run]
service = "scripts/run_service.py"
gui = "scripts/run_gui.py"
start_after = []
